<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable name="event" type="com.chyrkov.devoxxdemo.main.adapter.DevoxxAdapterItem"/>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:background="@color/colorPrimary"
            android:layout_height="wrap_content">

        <TextView
                android:id="@+id/item_name_textview"
                android:text="@{event.name()}"
                app:layout_constraintTop_toTopOf="parent"
                android:layout_marginTop="@dimen/margin_small"
                app:layout_constraintStart_toStartOf="parent"
                android:layout_marginStart="@dimen/margin_large"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textStyle="bold"
                android:textColor="@color/color_text_white"
                android:textSize="@dimen/font_size_default"
                tools:text="Registration &amp; Breakfast"/>

        <TextView
                android:id="@+id/time_textview"
                tools:text="From 9:00AM - 9:30AM"
                android:text="@{`From ` + event.timeBounds()}"
                app:layout_constraintTop_toBottomOf="@id/item_name_textview"
                app:layout_constraintStart_toStartOf="@id/item_name_textview"
                android:layout_marginTop="@dimen/margin_large"
                android:layout_marginBottom="@dimen/margin_small"
                app:layout_constraintBottom_toBottomOf="parent"
                android:textSize="@dimen/font_size_default"
                android:textColor="@color/color_text_white"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>